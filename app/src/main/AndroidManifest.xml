<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.fei.prointesomovil">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CancelarCitaActivity"></activity>
        <activity android:name=".InformacionConsultaActivity" />
        <activity android:name=".ConsultasActivity" />
        <activity android:name=".InformacionDietasActivity" />
        <activity android:name=".DietasActivity" />
        <activity android:name=".InformacionCitasActivity" />
        <activity android:name=".CitasActivity" />
        <activity android:name=".InformacionPacienteActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>